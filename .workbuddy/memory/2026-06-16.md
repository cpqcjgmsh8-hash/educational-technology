# 2026-06-16 工作日志

## Railway 部署成功 ✅
- 项目「国家开放大学教育技术学课程平台」后端 API 代理服务器成功部署到 Railway
- GitHub 仓库：https://github.com/cpqcjgmsh8-hash/educational-technology（Public）
- **关键修复**：`app.listen(PORT, '0.0.0.0')` 绑定所有网络接口，解决了持续多轮的 502 Bad Gateway 问题
- **架构调整**：
  - 删除根目录多余 `server.js` 转发文件，`package.json` 的 start 直接指向 `server/server.js`
  - `railway.json` 去掉 `startCommand`，让 Nixpacks 自动检测 `npm start`
  - `.gitignore` 排除 `resource/`（251MB）和 `server/node_modules/`
  - `.env.example` 清理真实密钥为占位符
- **环境变量**：COZE_TOKEN, BOT_ID, ASSISTANT_BOT_ID, TEACHER_BOT_ID, DASHSCOPE_API_KEY
- **API 端点**：/api/chat, /api/chat/stream, /api/chat/assistant/stream, /api/chat/teacher/stream, /api/mindmap/generate
- **待完成**：前端 HTML 中 localhost:8000 需替换为 Railway 域名
